import { } from './constants';

const initialState = {
  data: [],
  filter: {}
}

export function homeReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

